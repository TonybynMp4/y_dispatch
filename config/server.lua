return {
    useNpwd = true, -- Use npwd for 911/912 calls (send a text to the number and the message is sent to the dispatch)
    versionCheck = true -- Get notified when a new version is available on GitHub
}